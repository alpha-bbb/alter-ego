services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /var/log/alter-ego:/var/log/alter-ego
    ports:
      - "50051:50051"
    tty: true
    env_file: .env
    command: >
      ash -c "go run main.go"
